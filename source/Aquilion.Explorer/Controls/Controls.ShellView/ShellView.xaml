<UserControl x:Class="Controls.ShellView.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Controls.ShellView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Canvas x:Name="TaskPaneContainer" Margin="8" Width="200px" Background="Aqua"/>
        <Grid Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="250" MaxHeight="500"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Canvas x:Name="PreviewPaneContainer" />
            <GridSplitter Grid.Row="0" Background="Black" Height="7" HorizontalAlignment="Stretch"
                          VerticalAlignment="Bottom"/>
            <Canvas x:Name="ListviewContainer" Grid.Row="1"/>
        </Grid>
    </Grid>
</UserControl>
